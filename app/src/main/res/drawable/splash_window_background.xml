<?xml version="1.0" encoding="utf-8"?>
<!-- Window background shown instantly at app launch (before Compose renders the
     animated SplashScreen). Uses the same deep-navy gradient for a seamless,
     premium opening animation with no white flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_bg_start" />
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="90"
                android:startColor="#0B0820"
                android:centerColor="#150F2D"
                android:endColor="#000000"
                android:type="linear" />
        </shape>
    </item>
</layer-list>
